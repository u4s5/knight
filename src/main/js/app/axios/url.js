import {baseUrl} from 'app/service/baseUrl'

const api = `${baseUrl}/api/`;

// url
export const request = `${api}request`;
export const requestStatus = `${api}request/status`;
export const requestHandled1 = `${api}request/handled1`;
export const requestHandled2 = `${api}request/handled2`;
export const requestAccepted1 = `${api}request/accepted1`;
export const requestAccepted2 = `${api}request/accepted2`;
export const requestMessage = `${api}request/message`;

// upload
// export const upload = `${api}upload`;
